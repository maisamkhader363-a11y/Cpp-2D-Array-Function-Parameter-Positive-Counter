# Cpp 2D Array Function Parameter Positive Counter
A C++ application focusing on multi-dimensional structures and static matrix manipulation. The program reads a 3x4 2D integer array inside main(), passes it as a parameter to a specialized function, and utilizes coordinated nested loops to traverse the matrix grid and return the exact frequency of positive numbers.
